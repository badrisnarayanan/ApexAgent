<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Primary_Color_Hex_Format</fullName>
    <active>true</active>
    <description>Primary Color must be a 6-digit hex color including the leading # (for example, #6147B0).</description>
    <errorConditionFormula>AND(
    NOT(ISBLANK(Primary_Color__c)),
    NOT(REGEX(Primary_Color__c, &quot;^#[0-9A-Fa-f]{6}$&quot;))
)</errorConditionFormula>
    <errorDisplayField>Primary_Color__c</errorDisplayField>
    <errorMessage>Primary Color must be a 6-digit hex color including the leading # (for example, #6147B0).</errorMessage>
</ValidationRule>
